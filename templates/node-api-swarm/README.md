# {{PROJECT_NAME}}

Generated by `pasha-stack`.

## Local API

```bash
cd backend/api
corepack enable
pnpm install
pnpm start
```

## Frontend

```bash
node frontend/build-blog.js
```

Open `frontend/index.html` for the homepage, `frontend/blog/` for posts, and `frontend/app/` for the app placeholder.

## Production

- API image: `{{GHCR_IMAGE}}`
- API domain: `{{API_DOMAIN}}`
- API local port on VPS: `{{API_PORT}}`
- Main domain: `{{MAIN_DOMAIN}}`
- App domain: `{{APP_DOMAIN}}`
- API service: `{{API_SERVICE_NAME}}`
- Mongo service: `{{MONGO_SERVICE_NAME}}`
- Swarm network: `{{NETWORK_NAME}}`
- Pages project: `{{PAGES_PROJECT_NAME}}`
- App Pages project: `{{APP_PAGES_PROJECT_NAME}}`

## Useful Commands

```bash
docker service ls
docker service logs {{API_SERVICE_NAME}} -f
docker service logs {{MONGO_SERVICE_NAME}} -f
curl -s http://127.0.0.1:{{API_PORT}}/health
```
